apiVersion: v1
kind: Service
metadata:
  name: {{ include "flowmaster_chart.fullname" . }}
  labels:
    {{- include "flowmaster_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flowmaster_chart.selectorLabels" . | nindent 4 }}
